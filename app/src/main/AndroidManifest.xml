<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.sigadmin">

    <permission android:name="INTERNET"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".layouts.ListSubField">
        </activity>
        <activity android:name=".layouts.OtpActivity"/>
        <activity android:name=".layouts.LoginActivity"/>
        <activity android:name=".layouts.UpdateField"/>
        <activity android:name=".layouts.FieldDetail"/>
        <activity android:name=".layouts.InputNewSubField"/>
        <activity android:name=".layouts.RegisterActivity"/>
        <activity android:name=".layouts.InputNewField"/>
        <activity android:name=".layouts.HomeAdmin">

            <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>